<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <EditText
        android:id="@+id/filter_text"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/tabui_search_p"
        android:ellipsize="end"
        android:hint="Search"
        android:maxLength="25"
        android:maxLines="1"
        android:minLines="1"
        android:paddingLeft="30dp"
        android:paddingRight="30dp"
        android:singleLine="true" />

    <ListView
        android:id="@+id/listview"
        android:layout_width="fill_parent"
        android:divider="@drawable/tabui_listdividerbar"
        android:layout_height="wrap_content"
        android:layout_below="@+id/filter_text" >
    </ListView>

    <Button
        android:id="@+id/button1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/filter_text"
        android:layout_alignParentRight="true"
        android:layout_marginRight="15dp"
        android:background="@drawable/tabui_search_clear" />

</RelativeLayout>